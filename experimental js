function turnLeft() {
  let turn;
  turn = [17, 3];
  let ary; 
  ary = 15
  if (turn == ary) {
  } else if() {
  } else {
  };
}

function turnRight() {
  let turn;
  turn = [17, 3];
  let ary;
  ary = 15;
  if (turn == ary) {
  } else if () {
  } else {
  }
}

function upLook() {
  let look;
  look = x 
  x = 15
  let ary;
  ary = [15, 0]
  if (look == ary) {
  } else if () {
  } else {
  }
}


